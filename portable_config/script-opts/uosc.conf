# uosc upstream knobs we override
# (Liquid Glass-specific options moved to script-opts/liquid-glass.conf)
#
# Currently we accept uosc's stock defaults. Adjust here if you need to override
# something uosc-specific — see uosc's own docs for option names.
