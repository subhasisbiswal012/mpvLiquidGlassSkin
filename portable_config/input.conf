# Liquid Glass skin: override default mouse wheel behavior.
# Default mpv maps scroll to volume — we remap to seek so the skin's
# own scroll zones handle volume (only on the volume block).
WHEEL_UP    no-osd seek  5 relative+exact
WHEEL_DOWN  no-osd seek -5 relative+exact
